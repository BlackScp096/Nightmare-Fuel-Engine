package util;

import backend.LogHandler;

class EngineUpdateChecker {
    public static function checkForUpdates():Void {
        // simple stub to denote an update check
        try {
            LogHandler.log("EngineUpdateChecker: checked for updates (stub).");
        } catch (e:Dynamic) {}
    }
}
